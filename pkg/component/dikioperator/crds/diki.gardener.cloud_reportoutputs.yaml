---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: reportoutputs.diki.gardener.cloud
spec:
  group: diki.gardener.cloud
  names:
    kind: ReportOutput
    listKind: ReportOutputList
    plural: reportoutputs
    shortNames:
    - ro
    singular: reportoutput
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReportOutput describes a report output.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec contains the specification of this report output.
            properties:
              output:
                description: Output describes a specific output of a compliance scan.
                properties:
                  configMap:
                    description: ConfigMap contains the configuration for exporting
                      the report to a ConfigMap.
                    properties:
                      namePrefix:
                        default: compliance-scan-report-
                        description: |-
                          NamePrefix is the prefix for the generated ConfigMap name.
                          Defaults to "compliance-scan-report-".
                        type: string
                      namespace:
                        default: kube-system
                        description: |-
                          Namespace is the namespace where the ConfigMap will be created.
                          Defaults to `kube-system`.
                        type: string
                    type: object
                type: object
            required:
            - output
            type: object
        type: object
    served: true
    storage: true
